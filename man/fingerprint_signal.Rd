% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{fingerprint_signal}
\alias{fingerprint_signal}
\title{Extract the 5D structural fingerprint of a signal}
\usage{
fingerprint_signal(client, signal, sampling_rate, domain = "generic",
                   baseline = NULL)
}
\arguments{
\item{client}{An \code{alphainfo_client}.}
\item{signal}{Numeric vector.}
\item{sampling_rate}{Sampling rate in Hz.}
\item{domain}{Calibration domain.}
\item{baseline}{Optional reference signal (shortens the minimum
sample requirement from \code{MIN_FINGERPRINT_SAMPLES} to
\code{MIN_FINGERPRINT_SAMPLES_WITH_BASELINE}).}
}
\value{
Named list with \code{sim_local}, \code{sim_spectral}, \code{sim_fractal},
\code{sim_transition}, \code{sim_trend} (each \code{NA} when unavailable),
\code{fingerprint_available}, \code{fingerprint_reason}, plus a convenience
\code{vector} entry (numeric length-5) or \code{NULL} when incomplete.
}
\description{
Emits a warning when the signal is shorter than the minimum
threshold — the response will come back with
\code{fingerprint_available = FALSE}.
}
