% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{analyze_auto}
\alias{analyze_auto}
\title{Analyze a signal with automatic domain inference}
\usage{
analyze_auto(client, signal, sampling_rate,
             baseline = NULL, include_semantic = NULL,
             use_multiscale = NULL)
}
\arguments{
\item{client}{An \code{alphainfo_client}.}
\item{signal}{Numeric vector (min 10 samples).}
\item{sampling_rate}{Sampling rate in Hz.}
\item{baseline}{Optional reference signal.}
\item{include_semantic}{Include the semantic interpretation layer.}
\item{use_multiscale}{Enable multi-scale analysis.}
}
\value{
Same shape as \code{analyze_signal}. \code{domain_inference} is always
populated with \code{inferred}, \code{confidence}, \code{fallback_used},
and \code{reasoning}.
}
\description{
Syntactic sugar for \code{analyze_signal(client, ..., domain = "auto")}.
The server picks the most likely calibration from cheap signal
statistics; inspect \code{result$domain_inference} for the confidence
and reasoning behind the choice.
}
\examples{
\dontrun{
client <- alphainfo_client(Sys.getenv("ALPHAINFO_API_KEY"))
result <- analyze_auto(client, rnorm(500), sampling_rate = 250)
result$domain_applied
result$domain_inference$reasoning
}
}
